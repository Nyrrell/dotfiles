[Unit]
Description=Wake-on-LAN
Requires=network.target
After=network.target

[Service]
ExecStart=/bin/sh -c 'ethtool -s $(ip route show default | awk "/default/ {print \$5; exit}") wol g'
Type=oneshot

[Install]
WantedBy=multi-user.target
