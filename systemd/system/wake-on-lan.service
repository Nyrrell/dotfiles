[Unit]
Description=Wake-on-LAN
Requires=network.target
After=network.target

[Service]
ExecStart=/bin/sh -c 'ethtool -s $(ip route show default | head -n1 | cut -d" " -f5) wol g'
Type=oneshot

[Install]
WantedBy=multi-user.target
